# Implementation Plan — 2026-03-07 Code Review Fixes

Based on findings in [2026-03-07-code-review.md](2026-03-07-code-review.md). Each fix follows Red/Green/Blue TDD.

---

## Fix 1: notify.sh — Extract `launch_notify` helper

**Problem:** The notification launch block (build base args, conditionally add `-sender-app-path`, launch with env vars) is copy-pasted 4 times in `notify.sh`:
- Lines 454–484 (tmux with full context)
- Lines 489–501 (tmux parse failure)
- Lines 507–519 (tmux info empty)
- Lines 529–544 (non-tmux)

**Approach:** Extract a `launch_notify` shell function that accepts optional `-subtitle`, `-execute`, and `-activate` args via positional parameters. All 4 call sites become one-liners.

### Red — Write failing test

Add a new test case in `Tests/shell/unit/test-notify-activate.sh` that asserts `launch_notify` exists as a function:

```sh
case_start "U054" "notify.sh defines launch_notify helper function"
assert_file_contains "U054" "$SCRIPT" 'launch_notify()' \
  "notify.sh defines launch_notify function" \
  "notify.sh missing launch_notify function"
```

Add `U054` to `Tests/required-cases.txt` under `[unit]`.

### Green — Implement

Define `launch_notify` in `notify.sh` after the existing helpers, before the main logic:

```sh
launch_notify() {
  set -- \
    -title "Claude Code" \
    "$@" \
    -message "$MESSAGE" \
    -sound default \
    -group "claude-code" \
    -timeout "$NOTIFY_TIMEOUT" \
    -sender-mode "$SENDER_MODE" \
    -sender-bundle-id "$SENDER_BUNDLE_ID"
  if [ -n "$SENDER_APP_PATH" ]; then
    set -- "$@" -sender-app-path "$SENDER_APP_PATH"
  fi
  CLAUDE_NOTIFY_ISOLATE_HELPER_BUNDLE_ID="$NOTIFY_ISOLATE_HELPER_BUNDLE_ID" \
    CLAUDE_NOTIFY_ALLOW_NONISOLATED_RETRY="$NOTIFY_ALLOW_NONISOLATED_RETRY" "$NOTIFY" "$@" &
}
```

Replace each call site:

**Tmux with full context (lines 454–484):**
```sh
        launch_notify_extra=""
        if [ -n "$SESSION" ] && [ -n "$WINDOW_INDEX" ] && [ -n "$WINDOW_NAME" ]; then
          launch_notify_extra="-subtitle $SESSION:$WINDOW_INDEX.$WINDOW_NAME"
        fi
        # ... (execute cmd logic stays as-is, sets EXECUTE_CMD) ...
        if [ -n "$EXECUTE_CMD" ]; then
          launch_notify $launch_notify_extra -execute "$EXECUTE_CMD"
        else
          launch_notify $launch_notify_extra
        fi
```

**Tmux fallback/empty (lines 489–501, 507–519):**
```sh
        launch_notify
```

**Non-tmux (lines 529–544):**
```sh
  if [ -n "$ACTIVATE_OVERRIDE" ]; then
    launch_notify -activate "$ACTIVATE_OVERRIDE"
  else
    launch_notify
  fi
```

### Blue — Verify

Run all existing shell unit tests to confirm no regressions, especially U017–U039 which exercise the notification dispatch paths. The existing tests use fake `$NOTIFY` binaries and capture args — the helper refactor must preserve identical argument ordering.

**Files:** `notify.sh`, `Tests/shell/unit/test-notify-activate.sh`, `Tests/required-cases.txt`

---

## Fix 2: notify.sh — Extract `run_tmux_cmd` helper — DROPPED

**Blocked by test U031** which explicitly asserts that `notify.sh` uses explicit tmux query branches and does NOT define shared helpers like `run_tmux_query_with_logging` or `tmux_query`. A previous attempt to introduce shared helpers was deliberately reverted. Respecting that decision.

---

## Fix 3: main.swift — Delegate readPidFromFile

**Problem:** `readPidFromFile()` and `readPidFileContentFromFile()` both independently call `readPidFileRaw()` then `parsePidFileContent()`.

**Approach:** Make `readPidFromFile` delegate to `readPidFileContentFromFile`.

### Red — Write failing test

This is a pure refactor — the behavior is identical. Add an assertion in `UnitUtilityTests.swift` that exercises `parsePidFileContent` returning a valid PID (already covered by U042–U044). No new test case needed since existing tests cover the behavior. The refactor is validated by ensuring `make test-fast` still passes.

### Green — Implement

In `Sources/ClaudeNotify/main.swift`, replace lines 171–174:

```swift
// Before:
func readPidFromFile() -> pid_t? {
  guard let text = readPidFileRaw() else { return nil }
  return parsePidFileContent(text)?.pid
}

// After:
func readPidFromFile() -> pid_t? {
  readPidFileContentFromFile()?.pid
}
```

### Blue — Verify

`make test-fast` — all callers of `readPidFromFile()` (in `killPrevious` and `writePid`) exercise this path.

**Files:** `Sources/ClaudeNotify/main.swift`

---

## Fix 4: NotifyCore.swift — Fix TOCTOU in prepareSpoofHelper

**Problem:** Lines 721–724 and 742–744 check `fileExists` before `removeItem`. This is a TOCTOU race — the file could appear or disappear between the check and the operation.

**Approach:** Remove the existence checks; use `try?` to attempt removal directly.

### Red — Write failing test

Add a test in `UnitUtilityTests.swift` that exercises `prepareSpoofHelper` when the helper executable already exists (verifying it gets replaced). The existing test infrastructure uses injected `FileManager` and signer, so this test can use a temp directory:

```swift
func test_U055_prepareSpoofHelperReplacesExistingExecutable() throws {
  let tmp = FileManager.default.temporaryDirectory
    .appendingPathComponent("test-U055-\(ProcessInfo.processInfo.processIdentifier)")
  defer { try? FileManager.default.removeItem(at: tmp) }

  // Create a fake source executable.
  let sourceExec = tmp.appendingPathComponent("source-exec")
  try FileManager.default.createDirectory(at: tmp, withIntermediateDirectories: true)
  try "original".write(to: sourceExec, atomically: true, encoding: .utf8)
  try FileManager.default.setAttributes(
    [.posixPermissions: 0o755], ofItemAtPath: sourceExec.path)

  let sender = SenderAppInfo(
    bundleID: "com.test.sender",
    displayName: "Test",
    appURL: URL(fileURLWithPath: "/Applications/Test.app"),
    iconFile: nil)

  var signerCalled = false
  let options = HelperPreparationOptions(
    isolateHelperBundleID: false,
    baseBundleID: "com.test.notify",
    baseDirectories: [tmp],
    fileManager: .default,
    signer: { _, _ in signerCalled = true })

  // First call creates the helper.
  let exec1 = try prepareSpoofHelper(
    sender: sender, sourceExecutablePath: sourceExec.path, options: options)
  XCTAssertTrue(FileManager.default.fileExists(atPath: exec1.path))

  // Overwrite source with new content.
  try "replaced".write(to: sourceExec, atomically: true, encoding: .utf8)

  // Second call should replace without error.
  signerCalled = false
  let exec2 = try prepareSpoofHelper(
    sender: sender, sourceExecutablePath: sourceExec.path, options: options)
  XCTAssertTrue(signerCalled)
  let content = try String(contentsOf: exec2, encoding: .utf8)
  XCTAssertEqual(content, "replaced")
}
```

Add `U055` to `Tests/required-cases.txt`.

### Green — Implement

In `Sources/NotifyCore/NotifyCore.swift`, replace the executable copy block (lines 721–724):

```swift
// Before:
if fm.fileExists(atPath: helperExecutable.path) {
  try fm.removeItem(at: helperExecutable)
}
try fm.copyItem(atPath: sourceExecutablePath, toPath: helperExecutable.path)

// After:
try? fm.removeItem(at: helperExecutable)
try fm.copyItem(atPath: sourceExecutablePath, toPath: helperExecutable.path)
```

And the icon copy block (lines 742–744):

```swift
// Before:
if fm.fileExists(atPath: destIcon.path) {
  try fm.removeItem(at: destIcon)
}
try fm.copyItem(at: sourceIcon, to: destIcon)

// After:
try? fm.removeItem(at: destIcon)
try fm.copyItem(at: sourceIcon, to: destIcon)
```

### Blue — Verify

`make test-fast` — existing integration tests I101+ exercise the spoof helper preparation paths.

**Files:** `Sources/NotifyCore/NotifyCore.swift`, `Tests/NotifyCoreTests/UnitUtilityTests.swift`, `Tests/required-cases.txt`

---

## Fix 5: NotifyCore.swift — Optimize icon fallback scan

**Problem:** Line 527 sorts the entire Resources directory listing then filters for `.icns`. Sorting all files is unnecessary when most are not icons.

**Approach:** Filter first, then sort.

### Red — Write failing test

Extend the existing `test_U015_iconCandidatesNormalizesAndDeduplicates` or add a new test for `resolveSenderIconFile` that exercises the fallback path (no `CFBundleIconFile` in Info.plist, icon discovered by directory scan):

```swift
func test_U056_resolveSenderIconFileFallbackScansResourcesDirectory() throws {
  let tmp = FileManager.default.temporaryDirectory
    .appendingPathComponent("test-U056-\(ProcessInfo.processInfo.processIdentifier)")
  let resourcesDir = tmp.appendingPathComponent("Contents/Resources")
  defer { try? FileManager.default.removeItem(at: tmp) }

  try FileManager.default.createDirectory(at: resourcesDir, withIntermediateDirectories: true)
  // Create a non-icon file and two icon files.
  try "".write(to: resourcesDir.appendingPathComponent("readme.txt"),
               atomically: true, encoding: .utf8)
  try "".write(to: resourcesDir.appendingPathComponent("Zebra.icns"),
               atomically: true, encoding: .utf8)
  try "".write(to: resourcesDir.appendingPathComponent("AppIcon.icns"),
               atomically: true, encoding: .utf8)

  // Info.plist with no icon keys — forces fallback scan.
  let info: [String: Any] = ["CFBundleName": "Test"]
  let result = resolveSenderIconFile(appURL: tmp, info: info)
  // Should return the alphabetically first .icns file.
  XCTAssertEqual(result, "AppIcon.icns")
}
```

Add `U056` to `Tests/required-cases.txt`.

### Green — Implement

In `Sources/NotifyCore/NotifyCore.swift`, replace line 527:

```swift
// Before:
return files.sorted().first { $0.lowercased().hasSuffix(".icns") }

// After:
return files.filter { $0.lowercased().hasSuffix(".icns") }.sorted().first
```

### Blue — Verify

`make test-fast` — the new test and existing icon-related tests confirm correctness.

**Files:** `Sources/NotifyCore/NotifyCore.swift`, `Tests/NotifyCoreTests/UnitUtilityTests.swift`, `Tests/required-cases.txt`

---

## Verification checklist

After implementing all fixes:

```sh
cd hooks/claude-notify
make build
make test-fast
```

All existing tests must continue to pass. New test case IDs (U054, U055, U056) must be added to `Tests/required-cases.txt`.
