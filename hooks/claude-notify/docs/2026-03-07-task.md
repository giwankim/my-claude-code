# Task Tracker — 2026-03-07 Code Review Fixes

Derived from [2026-03-07-plan.md](2026-03-07-plan.md).

---

## Fix 1: notify.sh — Extract `launch_notify` helper

- [x] RED: Add test U054 asserting `launch_notify()` exists in notify.sh
- [x] RED: Add U054 to `Tests/required-cases.txt`
- [x] RED: Verify test fails
- [x] GREEN: Define `launch_notify` function in notify.sh
- [x] GREEN: Replace tmux full-context call site (lines 454-484)
- [x] GREEN: Replace tmux parse-failure call site (lines 489-501)
- [x] GREEN: Replace tmux info-empty call site (lines 507-519)
- [x] GREEN: Replace non-tmux call site (lines 529-544)
- [x] GREEN: Verify test passes
- [x] BLUE: Run full shell unit tests, confirm no regressions

## Fix 2: notify.sh — Extract `run_tmux_cmd` helper — DROPPED

- [x] Documented as blocked by test U031 (no shared tmux query helpers)

## Fix 3: main.swift — Delegate readPidFromFile

- [x] RED: Confirm existing tests U042-U044 cover the behavior
- [x] GREEN: Rewrite `readPidFromFile()` to delegate to `readPidFileContentFromFile()?.pid`
- [x] GREEN: Verify all tests pass
- [x] BLUE: Run `make test-fast`, confirm no regressions

## Fix 4: NotifyCore.swift — Fix TOCTOU in prepareSpoofHelper

- [x] RED: Add test U055 exercising prepareSpoofHelper with existing executable
- [x] RED: Add U055 to `Tests/required-cases.txt`
- [x] RED: Verify test (passes with old code — behavior-preserving change)
- [x] GREEN: Replace `fileExists` + `removeItem` with `try? removeItem` for executable copy
- [x] GREEN: Replace `fileExists` + `removeItem` with `try? removeItem` for icon copy
- [x] GREEN: Verify all tests pass
- [x] BLUE: Run `make test-fast`, confirm no regressions

## Fix 5: NotifyCore.swift — Optimize icon fallback scan

- [x] RED: Add test U056 for `resolveSenderIconFile` fallback directory scan
- [x] RED: Add U056 to `Tests/required-cases.txt`
- [x] RED: Verify test (passes with old code — behavior-preserving change)
- [x] GREEN: Change `files.sorted().first { ... }` to `files.filter { ... }.sorted().first`
- [x] GREEN: Verify all tests pass
- [x] BLUE: Run `make test-fast`, confirm no regressions

## Final Verification

- [x] `make build` succeeds
- [x] `make test-fast` passes (176 tests)
- [x] New case IDs U054, U055, U056 added to `Tests/required-cases.txt`

---

## PR Review Feedback (PR #13)

### Remove dead `LAUNCH_EXTRA` variable (notify.sh:471)
- [x] Delete unused `LAUNCH_EXTRA=""` assignment

### Strengthen test U055 — assert helper path reuse
- [x] Add `XCTAssertEqual(exec2, exec1)` to verify in-place replacement

### Strengthen test U056 — use lexically earlier non-.icns fixture
- [x] Change `"readme.txt"` to `"AAA.txt"` so test catches missing .icns filter

### Verify
- [x] `make build && make test-fast` passes
